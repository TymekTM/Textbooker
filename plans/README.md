# Implementation Plans - Test coverage for key regression surfaces + frontend E2E

Generated by the improve skill on 2026-08-26, at commit `32efc8d`, scoped by
the operator to: **duże pokrycie kluczowych elementów pod regresy +
zautomatyzowane testy frontendowe e2e**. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

Context in one line: the repo has 604 commits, ~19 audited regression-fix
commits, and zero automated tests - these plans build the net (unit →
integration → E2E) over exactly the surfaces that historically broke:
ItemManager school isolation, page-handler privacy/authz, the photo pipeline,
decimal price binding, and the HTMX/JS add-dialog and browse-filter UX.

## Why this directory exists (and when it goes away)

These plans are the working queue for a five-part test effort executed as a
stack of branches and PRs. They stay in the repo while the stack is in
flight because each open PR's review context (regression history, STOP
conditions, known-bug notes like the price-binding one below) lives here,
not in the tests it produced. The plans are one-time executor artifacts:
plan 002's output already rode the stack into this branch (PR #88);
003 and 004 were executed on other stack branches and reach main
through the chain PR #86 merges on top of, not through this PR.
PR #86 (plan 005) merges last, so that PR deletes this directory - the
tests and workflows are the durable output.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Test foundation - xUnit project, WAF host, CI gate | P1 | M | - | DONE (this PR, #87) |
| 002  | ItemManager characterization tests | P1 | M | 001 | DONE (PR #88, in this branch) |
| 003  | Page-handler integration regression tests | P1 | L | 001 | DONE (other stack branch, not in this PR) |
| 004  | Photo pipeline tests | P2 | M | 001 | DONE (other stack branch, not in this PR) |
| 005  | Playwright E2E suite (in-process host) | P2 | L | 001 | IN PROGRESS (PR #86) |

002, 003 and 004 are mutually independent after 001 and can run in parallel
branches. 005 is independent of 002-004 but benefits from landing after 003
(it copies `AuthHttpClient` patterns; not a hard dependency).

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 002/003/004/005 all build on 001's fixtures
  (`CustomWebApplicationFactory`, `TestSeed`, `FakeEmailSender`,
  `S3Recorder`, `public partial class Program`, the SQLite branch in
  `MigrateDatabaseAsync`). 001 must merge first.
- 003's `AuthHttpClient` helper gets copied/reused by 004; land 003 before
  004 or copy the helper into 004's branch independently.

## Known state at planning time (2026-08-26)

- **Live bug, deliberately not fixed by these plans**: decimal price binding
  is culture-dependent (query `12,50` → 1250; form `12.50` → error). The fix
  exists on local unmerged branch `fix/k6-invariant-price-binding`
  (commit `e009560`; since merged to main as PR #83). Plans 003 and 005 carry
  Skip-marked tests that turn on when that branch merges - un-skip them in the
  merge PR.
- The operator's working tree had an uncommitted one-line Serilog log-level
  diff in `Booker/Program.cs` (k6 leftover). Executors: see plan 001's
  "Known local-state warning".
- Untracked `nul` file at repo root (Windows reserved name) - operator
  cleanup; no plan touches it.

## Findings considered and rejected

- **`TimeProvider` injection to kill ~18 `DateTime.Now` sites** (ItemManager,
  SessionCacheManager, SchoolService, Login/Admin handlers): worthwhile but
  a production refactor, not a tests prerequisite - plans 002/003 assert
  ordering, not wall-clock values. Revisit when a feature actually needs
  deterministic time (lockout windows, session expiry).
- **Making the app's DbContext provider config-driven**: WAF can swap the
  provider via `RemoveAll<DbContextOptions>` without touching Program.cs; a
  `Database:Provider` config key is unnecessary surface. Rejected for now.
- **Hardening `SessionCacheManager` concurrency (plain `Dictionary` in
  singleton cache) and re-checking lockout mid-session**: real, but a
  behavior change outside test scope; the per-class-factory test design
  sidestops it. Track separately if admin lockout tests flake.
- **Fixing `TrackViewAsync`'s concurrent-duplicate race on main**: fix lives
  on unmerged `feature/wcag-2.2-aa` (`817cb61`); plan 002 notes the follow-up
  test for when it merges.
- **CI on `release` branch / deploy gating**: deploy.yml stays as is; the
  new build-test workflow gates PRs and main, which is where regressions
  enter.
- **Cleanup batch (unused `Faker.Net` package, dead
  `BookerIdentityDbContextConnection` string, dead `testingPhase` flag,
  unused `IpRateLimitAllMethods` policy, README's stale Blazor claim)**:
  real observations from the audit, none block the test plans; fold into a
  future chore PR.

## What was NOT audited (scope note)

The run was scoped to test coverage. Correctness/security/perf categories
were only incidentally inspected; the live price-binding bug surfaced via
the regression-history mining, not a full sweep. A separate broad audit can
follow.
